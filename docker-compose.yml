version: '3.8'

services:
  # --- ฐานข้อมูล ---
  postgres:
    image: postgres:16-alpine
    container_name: bl1nk-skills-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: bl1nk_skills
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
  
  # --- Caching ---
  redis:
    image: redis:7-alpine 
    container_name: bl1nk-skills-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

  # --- แอปพลิเคชัน Server ---
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bl1nk-skills-server
    restart: unless-stopped
    ports:
      - '3000:3000' 
    depends_on:
      - postgres 
      - redis
    environment:
      DATABASE_URL: postgresql://postgres:password@postgres:5432/bl1nk_skills
      REDIS_URL: redis://redis:6379
      NODE_ENV: production
      # 🚨 เพิ่มตัวแปรสภาพแวดล้อมอื่นๆ ที่จำเป็นสำหรับ Server (เช่น API Keys) ที่นี่
      # ตัวอย่าง:
      # ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}

volumes:
  postgres_data:
  redis_data:
